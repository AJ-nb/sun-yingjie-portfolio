# 孙英杰作品集交付

## 交付内容

- 网站地址：<https://sun-yingjie-portfolio.ajhhq.chatgpt.site>。目前为仅本人可访问的线上预览。
- 私人仓库：<https://github.com/AJ-nb/sun-yingjie-portfolio>，默认分支 `portfolio-redesign`，已核实私人可见性。
- 完整排版作品集：`deliverables/portfolio/sun-yingjie-portfolio.pdf`，40 页、30 个案例介绍、6 个细节页、可点击目录、30 个书签及案例网页链接。
- 专业简历：`deliverables/resume/sun-yingjie-resume.pdf`，一页 A4；同目录 `sun-yingjie-resume.docx` 为可编辑正文。
- 原创三维资产：`avatar/sun-yingjie-avatar-draft-v2.blend` 与 `web/public/models/avatar.glb`，另有正面、侧面、背面、四视图和转台验证。
- 设计与维护：根目录 `DESIGN.md`、`README.md`；来源覆盖与署名见 `docs/source-coverage.md`、`private/sources/coverage.json`。

## 网站能力

30 个案例、60 份中英文正文，覆盖商业空间、工业与产品、品牌视觉、数字产品及创作实验。支持分类、搜索、独立案例链接、图片放大、视频播放、语言切换、履历联系入口和两份 PDF 下载。

以用户选定的 sen 人物主导结构为基础；手机有底部导航和安全区域，减少动态效果时默认显示用户提供的静态形象，三维模型按需加载。原 sen 人物及个人素材已替换，MIT 说明保留。

## 验证与边界

- 本地生产预览完成 14 项浏览器检查，包含 30 条案例路由、129 张案例图片、筛选与返回状态、视频、下载、键盘灯箱以及 320 / 390 / 1440 像素布局。正常流程无控制台或页面异常；详细记录见 `browser-qa.md`。
- 从私人 GitHub 仓库重新检出后完成依赖安装、内容检查、类型检查、代码检查和构建。Git LFS 原件校验通过，最终校验记录见 `clean-checkout-verification.json`。
- 最终网站地址与最初预留地址不同，已修正所有交付链接。40 页作品集和1页简历重新渲染后与已审阅版逐像素一致；DOCX 除超链接关系外的归档条目均逐字节一致。详见 `final-url-verification.json`。
- 2026-09-15 02:18（Asia/Shanghai）线上最终版本核验通过：带本人预览身份的首页、模型、两份 PDF、静态人物图、三个运行资源及媒体清单共9项请求均为 HTTP 200；8 个文件与本地发布包逐字节一致，首页的最终网址与入口资源正确。匿名请求返回 HTTP 401，符合目前私人预览设置。证据见 `remote-delivery-verification.json`。
- 发布包仅含 164 项选入媒体和当前页面资源；原始文档、参考图档案、提示词、私人来源与 Blender 原件存于私人仓库，不进入网站构建。
- 159 条历史来源记录、128 页 PDF、21 页 PPTX 已逐项记录去向。11 个大型工程原件合计 23,283,668,869 字节，保留本地原路径，并记录校验值。
- 三维文件有真实空间几何、独立眼球、材质和镜头动画，技术结构已经验证。相似度、发型轮廓及侧面比例仍是艺术近似，尚未获得用户定稿确认。
- 未实测实体手机、Safari、屏幕阅读器、移动 GPU 帧率与电池消耗；相关限制不以模拟尺寸检查代替。

## 尚需完成的确认

已确认的方案要求人物相似度安排一次集中审阅，确认后继续公开发布。该审阅问题已提交，目前尚未收到答复。因此此交付保留私人线上预览，不能声称网站已经匿名公开。

人物审阅图：`web/public/images/avatar/multi-angle.png`。确认采用当前三维人物后，开放公众访问并验证匿名访问；若选择静态形象先发布，则调整首屏默认形态后再检查发布。简历和作品集的线上链接在开放公众访问前，仅能由站点本人账号访问。

## 后续维护

修改中英文案例时同步分类、署名和媒体。先运行 README 中的内容、类型、代码和构建检查；新增媒体需进入选择清单。保持私人原始档案和公开网站资源分离。

使用干净检出目录构建，再通过 `scripts/prepare_site_release.py` 创建新的静态发布目录，避免将旧版本生成文件混入。不要直接把完整私人仓库作为 Sites 的公开构建上传。凭据只在单次操作内使用，未写入项目文件。
