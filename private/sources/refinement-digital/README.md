# Brand and digital refinement evidence

Scope: Biyuan, YELISI, Periastra, Lensflow, Yantai, Formline, Resume Formatter, Visual Archive, Eagle Aesthetic Atlas, image-edit study, Xintiao, and XHS methods. Existing product/rendering cases are outside this agent's writes.

## Evidence separation

- Biyuan participation in brand, website, interaction and development is a statement confirmed by Sun Yingjie in the parent task. Public captures demonstrate the visible website, not an individual code contribution map or service benchmark.
- Biyuan was read without account credentials. Four actual screenshots were captured with the dedicated agent-browser CLI on 2026-09-15: desktop home, model directory, a gpt-image search state, and mobile home. The browser was then closed. CUA had failed to create a tab and reported a connection error on recovery.
- Biyuan website HTML was retrieved successfully (HTTP 200; final URL https://biyuan.ai/). The local project collection did not contain an independent Biyuan website source. Lensflow and Resume Formatter source show separate Biyuan integrations; they are not represented as website source.
- Existing Lensflow v0.3.0 screenshots are saved demo captures. Yantai uses a study fixture. Visual Archive screenshots show local measurement and an ungenerated model dossier. These are retained with their limitations rather than replaced with artificial success states.
- YELISI and Periastra use the existing authenticated AJNB source archive. Product dimensions and application sizes remain concept/proposed-check evidence, not manufacturing proof.
- XHS's new workflow is an original method diagram derived from requirements. The public case uses a PNG rendered from the preserved source SVG for raster/PDF compatibility; its caption says it is not a running interface. The source homepage is a Foundation Shell at P0 / Task 4.

## Xintiao isolation and verification

Original project: D:/OneDrive/桌面/文件/项目/WB/学习/today-salary-miniapp. It was treated as read-only.

The ignored xintiao-isolated directory is a copy for local validation and capture. Its project name and AppID were changed to a capture name and touristappid. Only the copy received a root tsconfig.json extending the original miniprogram/tsconfig.json, because the original directory lacks the root file referenced by its package scripts. No salary or UI implementation was changed.

The first test attempt had 311 passing assertions but a failed architecture suite because project.private.config.json had not yet been copied. The file was inspected and contains non-secret compiler/environment settings. After copying it, the final run passed **338 tests across 19 test files**, with zero failures. The original 295-test README count is historical and was not used as the current result.

The copied build script passed with **14 pages, 7 shared main-package components and zero failures**. Its report is xintiao-build-report.json. This is project validation; it does not equal WeChat DevTools native compilation or real-device verification. The current state code uses schema version 5 while the README still says 4.

Native CLI attempt: installed WeChat DevTools 2.02.2608060 recognized the auto command, but rejected it before launching because the IDE service port is disabled. No service/security setting was changed. Native UI import then succeeded in guest mode. Five direct, unretouched window screenshots document onboarding, live home, salary details, calendar and appearance settings. The public case uses the home capture as its cover. Inputs are the default demonstration values, not personal financial data.

Three-step setup completed; home earnings updated and salary details/calendar navigation worked. Dark selection saved but appearance, calendar and home remained light, including after an embedded-simulator reload. This is a recorded unresolved rendering issue, not a verified dark theme. Console excerpts show guest-mode wx.operateWXData limits, an SDK webapi_getwxaasyncsecinfo failure and preload warnings. All cumulative errors were not individually classified; zero-error runtime is not claimed. Exact conditions and evidence paths are recorded in xintiao-native-runtime.json.

The imported project and floating simulator were closed normally. The original, automatically restored weight-bmi-tracker window was left untouched. Real-device, WeChat publishing and payroll accuracy remain outside the verified scope.

## Files and exclusions

verification.json records source hashes, exact public media hashes, language pairs and content checks. evidence/ contains selected source text copied without modification. Original validation reports remain alongside it.

Local .gitignore rules exclude npm caches, tool dependencies and the complete isolated working copy. No generated dependency tree or copied working source repository belongs in the published site.

The first three authoring helpers under scripts/digital-r2 are historical content-generation passes. Do not rerun them over the final native screenshot edits. verify-content.mjs validates final content and refreshes the evidence manifest. The normal full-site content validator must also pass before publication.

browser-entry-check.md records why the later, read-only attempt to identify an existing logged-in ChatGPT browser entry ended. No generation was submitted; operational details remain private.
